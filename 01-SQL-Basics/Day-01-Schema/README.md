# Day 1: Designing a Retail Store Database 🏪

## 🎯 Objective
Membangun fondasi database relasional untuk toko retail fiktif. Fokus utama hari ini adalah memahami **Data Integrity** (keutuhan data) melalui desain tabel yang tepat.

## 🛠️ Key Concepts Learned
| Konsep | Penjelasan Singkat |
| :--- | :--- |
| **CREATE TABLE** | Perintah dasar untuk membuat wadah data. |
| **PRIMARY KEY** | Memastikan setiap baris data punya identitas unik (tidak boleh duplikat). |
| **Data Types** | Memilih tipe data yang efisien: `INTEGER` untuk ID, `VARCHAR` untuk teks, `DECIMAL` untuk uang (agar presisi). |
| **Constraints** | Menggunakan `NOT NULL` agar data penting (seperti nama produk) tidak boleh kosong. |

## 📂 File Structure
- `retail_store_schema.sql`: Berisi query SQL untuk membuat tabel `customers`, `products`, dan `orders`.

---
*Next Step: Populating the database with dummy data (INSERT).*
